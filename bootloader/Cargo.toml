[package]
name = "bootloader"
version = "0.0.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
alloc = { path = "../libs/alloc" }
bootloader_api = { path = "../bootloader_api" }
serial = { path = "../libs/serial" }
uefi = { path = "../libs/uefi" }
x86_64 = { path = "../libs/arch/x86_64" }
